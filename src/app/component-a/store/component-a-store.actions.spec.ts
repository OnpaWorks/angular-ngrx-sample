import * as componentAStoreActions from './component-a-store.actions';

describe('loadLists', () => {
  // it('should return an action', () => {
  //   expect(componentAStoreActions.loadLists().type).toBe('[List] Load Lists');
  // });
});
